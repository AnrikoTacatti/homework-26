## 0. Контекст системы

Сервис такси - это ride-sharing платформа, которая соединяет пассажиров и
водителей, предоставляющих услуги перевозки.  
Это позволяет пользователям заказывать услуги с их телефона, подбирая к
ним ближайших доступных водителей.

- пользователи 
  водители, пасажири, менеджеры  

- сценарии использования  
  пользователь запрашивает поездку  
  водитель откликается на предложение  
  система находит предложение  
  оплата   
  пользователь дает оценку  

- масштаб 6M водителей, из них 3M активных, 20 млн пассажиров, 10 млн поездок
---

## 1. Компоненты (5-10 штук)

Для каждого компонента:
 
  Клиентское приложение — клиентское приложение. Отвечает за UI и взаимодействие с API через API Gateway. (Stateless)
  Сервис водителей (Driver Service) — приложение водителя. Отвечает за UI и взаимодействие с API через API Gateway. (Stateless)
  Passenger Service — сервис управления пассажирами. Отвечает за профиль пассажира: имя, телефон, настройки и другие данные пользователя.(Stateless)
  Driver Service — сервис управления водителями. Отвечает за профиль водителя, информацию об автомобиле и рабочее состояние водителя (Stateless)
  PostgreSQL — база данных (Хранит пользователей, поездки, платежи, историю.) (Stateful) 
  Redis — кэширует часто используемые данные (профили пользователей и водителей, тарифы, сесии пользователей) (Stateful)
  Redis Geo — геопространственное хранилище (Stateful)
  Kafka — очередь сообщений (брокер событий) (Передаёт события между сервисами: заказ создан, водитель принял заказ, поездка завершена) (Stateful)
  API Gateway — Единая точка входа для клиентов и водителей. Роутит запросы к внутренним сервисам (поездки, платежи, профили). (Stateless)
  Load Balancer — балансировщик нагрузки (Stateless)
  Внешняя платёжная система — внешняя интеграция (stateful)
  Сервис заказов (Order Service) —  создаёт заказ и управляет его жизненным циклом (Stateless)
  Payment Service - Платёжный сервис Обрабатывает оплату поездок, интегрируется с внешней платёжной системой (Stateless)
  Matching Service - Подбирает ближайшего свободного водителя (Stateless)
  Tracking Service - Передаёт координаты водителя клиенту в реальном времени (Stateless)
  Rating Service — обрабатывает оценки и отзывы пассажиров и водителей. (Stateless)
  Notification Service — сервис. Отправляет пассажирам и водителям уведомления о событиях поездки: создан заказ, водитель назначен, поездка началась или завершена. Получает события из Kafka. (Stateless)
  Analytics Service — сервис. Получает события из Kafka и обрабатывает данные для аналитики: поездки, действия пользователей, статусы заказов и другие метрики системы. (Stateless)

> Подсказка: начинайте с вопроса «какие компоненты я ожидаю увидеть внутри?»

---

## 2. Потоки данных (3-5 ключевых сценариев)
```text

Client (приложение) → API Gateway → Passenger Service → Redis  → ❌ Redis → PostgreSQL → ❌ PostgreSQL → Driver Service retry → ERROR 503 → Client 
                                                     ├── HIT  → Client (приложение)
                                                     └── MISS → PostgreSQL → Redis → Client (приложение)

                                            
Driver App → API Gateway → Driver Service → Redis → ❌ Redis → PostgreSQL(реплика) → ❌ PostgreSQL → Driver Service retry  → ERROR 503 → Driver App
                                             ├── HIT → Driver App
                                             └── MISS → PostgreSQL → Redis → Driver App                                                   

Client (приложение) → Load Balancer → API Gateway →
Order Service → Database (Orders DB PostgreSQL)

Client→ API Gateway → Order Service → DB(PostgreSQL)
                     ↓
                   Kafka (асинхронно) -> Notification Service
                                      -> Analytics Service
                     ↓
              Matching Service (асинхронно)
                     ↓
                   Redis Geo Cluster (geo) (несколько нод)
                     ↓
              Driver Service → Driver App

Driver App → API → Order Service → DB

Driver App → WebSocket → Tracking → Redis → Client App

Водитель нажал "Завершить поездку"

Driver App → API Gateway →  Order Service →  Payment Service →  Внешняя платёжная система → ❌ Платёжная система → Payment Service → retry → Payment failed
        │
        ▼
Order Service меняет статус заказа на COMPLETED
      │
      ▼
Kafka
 ├──► Notification Service
 ├──► Analytics Service
 └──► Client показывает экран оценки
 └──► Driver показывает экран оценки

Пользователь поставил оценку

Client → API Gateway → Rating Service → Ratings DB

❌ Kafka FAIL → Order Service → Outbox/PostgreSQL → retry → Kafka
❌ PostgreSQL Primary + Replica → retry → retry → ERROR 503 → App
❌ PostgreSQL Primary + Replica 1 → PostgreSQL Replica 2 → Driver Service → Driver App
```
---

## 3. Проблемы и решения

| Компонент                        | Проблема                                                                                 | Без него                                                                       | Trade-off                                                                                          |
| -------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- |
| **Load Balancer**                | Распределяет входящие запросы между экземплярами API Gateway                             | Один сервер перегружается, отказ приводит к недоступности системы              | ✅ Высокая доступность и горизонтальное масштабирование<br>❌ Дополнительная инфраструктура          |
| **API Gateway**                  | Единая точка входа, маршрутизация, авторизация, rate limiting                            | Клиентам пришлось бы знать адреса всех сервисов, усложнилась бы безопасность   | ✅ Централизованное управление<br>❌ Может стать узким местом                                        |
| **Passenger Service**            | Управляет профилем пассажира: имя, телефон, настройки                                    | Ответственность за профиль была бы размазана по другим сервисам                | ✅ Чёткая ответственность<br>❌ Дополнительные сетевые вызовы                                        |
| **Driver Service**               | Управляет профилем водителя, автомобилем и состоянием `online/offline`, `available/busy` | Логика водителей оказалась бы в Order Service                                  | ✅ Независимое развитие и масштабирование<br>❌ Дополнительное взаимодействие между сервисами        |
| **Order Service**                | Создаёт заказ и управляет жизненным циклом поездки                                       | Логика заказа была бы размазана по другим сервисам                             | ✅ Чёткая ответственность<br>❌ Дополнительные сетевые вызовы                                        |
| **Matching Service**             | Подбирает ближайшего свободного водителя                                                 | Логика поиска водителя оказалась бы в Order Service                            | ✅ Независимое масштабирование<br>❌ Дополнительный сервис                                           |
| **Redis Geo**                    | Быстро ищет ближайших водителей по координатам                                           | Поиск через PostgreSQL был бы значительно медленнее                            | ✅ Очень быстрый geo-поиск<br>❌ Данные в памяти, требуется репликация/синхронизация                 |
| **Tracking Service + WebSocket** | Передаёт координаты водителя клиенту в реальном времени                                  | Пришлось бы использовать polling, увеличивая нагрузку и задержки               | ✅ Мгновенные обновления<br>❌ Большое количество открытых соединений                                |
| **Payment Service**              | Обрабатывает оплату и взаимодействует с платёжным провайдером                            | Order Service пришлось бы напрямую заниматься платежами                        | ✅ Изоляция платёжной логики<br>❌ Дополнительный сетевой вызов                                      |
| **Внешняя платёжная система**    | Выполняет авторизацию и списание денежных средств                                        | Нужно было бы самостоятельно реализовывать интеграции с банками/провайдерами   | ✅ Готовая платёжная инфраструктура<br>❌ Зависимость от внешнего API                                |
| **Rating Service**               | Обрабатывает оценки и отзывы пассажиров и водителей                                      | Логика рейтингов была бы объединена с Order Service                            | ✅ Чёткая ответственность<br>❌ Дополнительный сервис                                                |
| **Notification Service**         | Отправляет уведомления на основе событий Kafka                                           | Order Service пришлось бы напрямую работать с каналами уведомлений             | ✅ Независимая обработка уведомлений<br>❌ Возможны задержки из-за асинхронности                     |
| **Analytics Service**            | Обрабатывает события поездок, заказов и действий пользователей                           | Аналитическая нагрузка легла бы на основные сервисы                            | ✅ Изоляция аналитической нагрузки<br>❌ Eventual Consistency и дополнительная обработка             |
| **Kafka**                        | Передаёт события между сервисами асинхронно                                              | Сервисы пришлось бы вызывать синхронно, отказ одного мог бы остановить цепочку | ✅ Масштабируемость и отказоустойчивость<br>❌ Eventual Consistency и усложнение архитектуры         |
| **Redis**                        | Кэширует профили, тарифы, сессии и другие часто используемые данные                      | Все запросы шли бы в PostgreSQL, увеличивая нагрузку                           | ✅ Быстрый доступ и снижение нагрузки на БД<br>❌ Возможны устаревшие данные, нужен TTL/invalidation |
| **PostgreSQL**                   | Надёжно хранит пользователей, поездки, платежи, рейтинги и историю                       | Данные были бы потеряны после перезапуска сервисов                             | ✅ ACID и долговременное хранение<br>❌ Горизонтальное масштабирование сложнее                       |



---

## 4. Вопросы к авторам архитектуры (минимум 5)

Почему поиск водителей реализован через Redis Geo, а не PostgreSQL?  
Почему Order Service и Matching Service разделены?  
Что произойдёт, если Kafka станет недоступна?  
Как предотвращается двойное назначение одного заказа двум водителям?  
Какой компонент станет первым узким местом при росте нагрузки в 10 раз?  

