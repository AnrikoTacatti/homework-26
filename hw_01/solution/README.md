## 0. Контекст системы

Сервис такси - это ride-sharing платформа, которая соединяет пассажиров и
водителей, предоставляющих услуги перевозки.  
Это позволяет пользователям заказывать услуги с их телефона, подбирая к
ним ближайших доступных водителей.
авторизация

- пользователей (водители, пасажири, менеджеры)  
- сценарии использования  
  пользователь запрашивает поездку  
  водитель откликается на предложение  
  система находит предложение  
  оплата   
  пользователь дает оценку  
- масштаб 6M водителей, из них 3M активных  
---

## 1. Компоненты (5-10 штук)

Для каждого компонента:

  Указал **название** и **тип** (сервис, БД, кэш, очередь, CDN, балансировщик, внешняя интеграция)  
  Клиентское приложение — сервис отвечает за авторизацию, находит предложение по локации, оплату, рейтирг  
  Сервис заказов (Order Service) — сервис  
  Сервис водителей (Driver Service) — сервис для таксистов предлагает клиентов по локации , время поездки , оплату ,рейтирг  
  PostgreSQL — база данных (Хранит пользователей, поездки, платежи, историю.)  
  Redis — кэш  
  Redis Geo — геопространственное хранилище  
  Kafka — очередь сообщений (брокер событий) (Передаёт события между сервисами: заказ создан, водитель принял заказ, поездка завершена)  
  Внешняя платёжная система — внешняя интеграция  
  API Gateway — Единая точка входа для клиентов и водителей. Роутит запросы к внутренним сервисам (поездки, платежи, профили).  
  Load Balancer — балансировщик нагрузки  
  Платёжный сервис Обрабатывает оплату поездок, интегрируется с внешней платёжной системой  
  Matching Service - Подбирает ближайшего свободного водителя  
  Tracking Service - Передаёт координаты водителя клиенту в реальном времени	  

> Подсказка: начинайте с вопроса «какие компоненты я ожидаю увидеть внутри?»

---

## 2. Потоки данных (3-5 ключевых сценариев)
```text
Client (мобильное приложение) → Load Balancer → API Gateway →
Order Service → Database (Orders DB PostgreSQL)

Client→ API Gateway → Order Service → DB(PostgreSQL)
                     ↓
                   Kafka (асинхронно) -> Notification Service
                                      -> Analytics Service
                     ↓
              Matching Service (асинхронно)
                     ↓
                   Redis (geo)
                     ↓
              Driver Service → Driver App

Driver App → API → Order Service → DB

Driver App → WebSocket → Tracking → Redis → Client App

Водитель нажал "Завершить поездку"

Driver App → API Gateway →  Order Service →  Payment Service →  Внешняя платёжная система
Order Service меняет статус заказа на COMPLETED
      │
      ▼
Kafka
 ├──► Notification Service
 ├──► Analytics Service
 └──► Client показывает экран оценки
 └──► Driver показывает экран оценки

Пользователь поставил оценку

Client → API Gateway → Rating Service → Ratings DB
```
---

## 3. Проблемы и решения

Load Balancer	
Распределяет входящие запросы между несколькими экземплярами API Gateway	 
Один сервер быстро перегрузится, отказ сервера приведёт к недоступности системы	  
✅ Высокая доступность и горизонтальное масштабирование   
❌ Дополнительная точка инфраструктуры

API Gateway	  
Единая точка входа, маршрутизация, авторизация, rate limiting	 
Клиентам пришлось бы знать адреса всех сервисов, безопасность усложнилась бы  
✅ Централизованное управление   
❌ Может стать узким местом, если не масштабировать 

Order Service	  
Создаёт заказ, хранит его состояние, управляет жизненным циклом поездки	 
Логика заказа была бы размазана по другим сервисам  
✅ Чёткая ответственность   
❌ Дополнительный сетевой вызов к другим сервисам   

Kafka	  
Передаёт события между сервисами без жёсткой зависимости	 
Все сервисы пришлось бы вызывать синхронно, отказ одного мог бы остановить остальных	 
✅ Масштабируемость и отказоустойчивость  
❌ Eventual Consistency и более сложная архитектура  

Matching Service	  
Подбирает ближайшего свободного водителя	 
Логика была бы размазана по другим сервисам  
✅ Независимое масштабирование   
❌ Дополнительный сервис  

Redis Geo	  
Быстро ищет ближайших водителей по координатам	  
Поиск через PostgreSQL был бы значительно медленнее	  
✅ Очень быстрый поиск   
❌ Данные в памяти, требуется синхронизация  

Driver Service	  
Управляет состоянием водителей (свободен, занят, офлайн)	  
Вся логика работы водителей оказалась бы в Order Service	  
✅ Независимое развитие   
❌ Дополнительное взаимодействие между сервисами  

Tracking Service + WebSocket	
Передаёт координаты водителя клиенту в реальном времени	  
Пришлось бы использовать постоянный polling, что увеличило бы нагрузку и задержки	  
✅ Мгновенные обновления   
❌ Нужно поддерживать большое количество открытых соединений  

Orders DB	  
Надёжно хранит информацию о заказах	  
После перезапуска системы все заказы были бы потеряны	  
✅ ACID и долговременное хранение   
❌ Горизонтальное масштабирование сложнее  

---

## 4. Вопросы к авторам архитектуры (минимум 5)

Почему поиск водителей реализован через Redis Geo, а не PostgreSQL?  
Почему Order Service и Matching Service разделены?  
Что произойдёт, если Kafka станет недоступна?  
Как предотвращается двойное назначение одного заказа двум водителям?  
Какой компонент станет первым узким местом при росте нагрузки в 10 раз?  

